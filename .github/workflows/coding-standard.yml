name: PHPCS on PR

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

jobs:
  phpcs:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4'  # or any version you are using
          tools: composer

      - name: Install dependencies
        run: |
          composer install

      - name: Determine changed files
        id: filter
        uses: dorny/paths-filter@v2
        with:
          list-files: shell
          filters: |
            php:
              - 'includes/**/*.php'
              - 'includes/**'

      - name: Debug Filter Output
        run: |
          echo "Filter php = ${{ steps.filter.outputs.php }}"
          echo "Matching files: ${{ steps.filter.outputs.files }}"
          echo "Filter Outputs: ${{ toJson(steps.filter.outputs) }}"
          echo "File Outputs: ${{ toJson(steps.filter.outputs.php_files) }}"
          echo "Filter Outputs: ${{ toJson(steps.filter) }}"
          echo "Filter Outputs: ${{ toJson(steps) }}"

      - name: List Changed Files
        run: |
          git diff --name-only ${{ github.event.before }} ${{ github.sha }}

      - name: Run PHPCS
        if: steps.filter.outputs.php == 'true'
        run: |
          ./vendor/bin/phpcs --standard=phpcs.xml ${GITHUB_WORKSPACE}/${{ steps.filter.outputs.php_files }}
